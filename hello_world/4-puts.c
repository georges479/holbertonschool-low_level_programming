#include <stdio.h>


/**
 * main - Entry point of the program
 *
 * This program prints a statement about art with proper grammar.
 *
 * Return: Always returns 0 to indicate success.
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
