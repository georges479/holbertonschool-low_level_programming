#include <stdio.h>
#include <main.h>
#include <ctype.h>

/**
 * _isupper - function that checks for uppercase character
 * 
 * Return: 1 if is uppercase, 0 if not
 */
int _isupper(int c)
{
    int c;

    if (isupper(c))
    {
        return (1);
    }
    else
    return (0);
}